kafka.broker.port = ${kafka.broker.port}
kafka.broker.topics = ${kafka.broker.topics}
kafka.broker.partition-count = ${kafka.broker.partition-count}
kafka.broker.logs-dir = ${kafka.broker.logs-dir}
#kafka.broker.host = ${kafka.broker.host}
#kafka.broker.broker-count = ${kafka.broker.broker-count}
#kafka.broker.controlled-shutdown = ${kafka.broker.controlled-shutdown}